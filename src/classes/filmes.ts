export class filme {
    nome: string
    ano: number 
    genero: string

    constructor(nome: string, ano: number, genero: string) {
        this.nome = nome
        this.ano = ano
        this.genero = genero
    }
} 